{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Cove",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/cove",  // your main package
      "cwd": "${workspaceFolder}",                          // 👈 force working dir = repo root
      "console": "integratedTerminal",
      "env": {
        "ENVIRONMENT": "dev"
      }
    }
  ]
}
